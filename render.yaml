services:
  - type: web
    name: memory-decay-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    rootDir: "backend (3)/backend"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
